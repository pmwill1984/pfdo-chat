meta:
  environment: python
  toolchain:
    name: pip
    version: "3.12"

run:
  command: cd backend && pip install -r requirements.txt && gunicorn --bind 0.0.0.0:80 main:app
  port: 80
# Updated Mon Aug 17 12:15:30 MSK 2026
