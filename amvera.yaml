meta:
  environment: python
  toolchain:
    name: pip
    version: "3.11"

build:
  requirements: backend/requirements.txt

run:
  command: cd backend && gunicorn --bind 0.0.0.0:80 main:app
  port: 80
