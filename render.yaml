services:
  - type: web
    name: pfdo-chat
    runtime: python
    buildCommand: pip install flask flask-cors requests
    startCommand: cd backend && python main.py
    envVars:
      - key: PORT
        value: 8000
